import type { FC } from 'react';

export const BlockPage: FC = () => {
  return (
    <div
      className={
        'flex flex-col items-center justify-center absolute top-0 left-0 w-full h-dvh z-50'
      }
    />
  );
};
